<!DOCTYPE html>
<html>
<head>
  <title>Splurty</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <meta name="viewport" content="width=device-width, initial-scale=1">
</head>
<body>
<header>

    <div class="col-xs-2">
    <%= link_to root_path, class: 'logo' do %>
      <div class="circle">
        <br />
        <br />
        Splurty!
      </div>
      <% end %>
    </div>

    <div class="col-sm-6 hidden-xs slogan">
      Splurting what you love.. or don't!
    </div>
      <div class="col-xs-9 col-xs-offset-1 col-sm-offset-0 col-sm-4 links">
      <!-- Button trigger modal -->
      <%= link_to 'Contribute', '#', data: {toggle: 'modal', target: '#myModal'} %>
      &nbsp;&nbsp;
      <%= link_to 'About', about_path %>
    </div>
  </header>

  <!-- Modal -->
  <div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
          <h4 class="modal-title" id="myModalLabel">Enter your own quote of what you love.. or hate</h4>
        </div>
                <%= simple_form_for Quote.new do |f| %>
          <div class="modal-body">
            <%= f.input :saying, label: "Enter your saying here:", placeholder: "Don't you love it when Splurty gives you a sample quote?" %>
            <br />
            <%= f.input :author, label: "Who is the author?", placeholder: "Splurty" %>
          </div>
          <div class="modal-footer">
            <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
            <%= f.submit 'Add Your Quote', class: 'btn btn-danger' %>
          </div>
        <% end %>
      </div>
    </div>
  </div>

  <hr />
  <br class="clear" />
<div class="container">
  <% if flash[:error].present? %>
      <div class="col-xs-10 col-xs-offset-1 alert alert-danger">
        <%= flash[:error].html_safe %>
      </div>
    <% end %>
  <%= yield %>
</div>
  <div class="footer">
    <div class="container">
      <div class="col-xs-12 col-sm-7">
        <%= link_to "Show me another quote!", root_path, class: 'next-quote' %>
      </div>
      <div class="hidden-xs col-sm-5 footer-blurb">
        Hacked together by <a href="https://github.com/CRossPhD">Christopher Ross</a>
      </div>
    </div>
  </div>



</body>
</html>
